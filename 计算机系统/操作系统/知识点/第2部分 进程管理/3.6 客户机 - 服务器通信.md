---
domain: 计算机系统
type: 概念
status: 已完成
course: 操作系统
chapter: 3
section: "3.6"
order: 306
cssclasses:
  - os-note
prerequisites:
  - "[[第三章 进程]]"
  - "[[3.5 IPC系统例子]]"
tags:
  - 操作系统
  - 进程管理
source: Silberschatz, Operating System Concepts (10th Edition)
created: 2026-07-16
updated: 2026-07-16
---

# 3.6 客户机/服务器通信

本节聚焦于**客户机/服务器通信**，是[[第三章 进程]]中的独立知识节点。

> [!summary] C/S 架构的通信机制
> 客户机/服务器架构是分布式系统的核心模式，主要通过套接字、远程过程调用（RPC）和管道三种机制实现通信。

## 3.6.1 套接字

> [!summary] 基本概念
> 套接字由 **IP 地址**和**端口号**组成，是网络通信的"端点"。每个 TCP 连接都由一对独一无二的套接字（源IP:源端口 + 目的IP:目的端口）来唯一标识。

**端口规则**

- **0～1023** 是传统的系统/众所周知端口范围，许多标准服务使用其中的端口（如 HTTP 80）；端口的服务登记与进程是否有权绑定是两个问题，权限规则取决于操作系统。
- 客户端发起连接时通常由操作系统从本机配置的**临时端口（ephemeral port）范围**选择端口；具体范围取决于操作系统，不能简单等同于“大于 1024”。

**Java 套接字 API 的类型**

| 类型 | 类名 | 特性 |
| --- | --- | --- |
| **TCP 套接字** | `java.net.Socket` | 面向连接的可靠传输 |
| **UDP 套接字** | `java.net.DatagramSocket` | 无连接、不可靠但高效的传输 |
| **多播套接字** | `java.net.MulticastSocket` | 允许一份数据发送给多个接收者 |

**日期服务器交互案例（C/S 架构）**

> [!example] 日期服务器的最小协议
> 服务器依次执行“创建套接字 → 绑定地址 → 监听 → 接受连接 → 写入时间字符串 → 关闭连接”；客户端执行“连接 → 循环读取直到 EOF → 解析或显示 → 关闭”。真实程序还必须处理短读/短写、超时、字符编码、并发连接和网络字节序；这些边界比只展示一次成功调用更重要。

> [!example] 对照代码
> [[MOC - 操作系统示例代码#第三章：进程与进程间通信|查看 Java 日期服务器与客户端]]。

**套接字的局限性**

> [!warning] 低级通信形式
> 套接字通信属于**低级的通信形式**。它本质上只是传递**"无结构的字节流"**。如果需要在进程间传递结构化数据（如对象、复杂记录），应用程序员必须自己编写序列化和反序列化的代码。

## 3.6.2 远程过程调用

> [!summary] 核心目标
> RPC 旨在让网络上的**远程过程调用**，对调用者来说感觉和**调用本地过程**一样。

**抽象与通信机制**

> [!mechanism] C/S 架构 + 消息传递
> RPC 采用**客户机-服务器**模型，建立在**消息传递**（IPC）之上。消息具有明确的结构（包含函数标识符和参数），而非无结构的数据包。

**存根（Stub）与参数封装（Marshaling）**

> [!mechanism] 透明调用流程
> 1. 客户端调用本地存根。
> 2. 客户端存根**封装（Marshaling）**参数（打包成网络可传输格式）。
> 3. 向服务器发送消息。
> 4. 服务端存根接收并解包，调用实际的服务例程。
> 5. 服务端将结果封装并发回，客户端存根接收并返回给调用者。

> [!tip] Windows 实现
> 在 Windows 系统中，使用 **MIDL（微软接口定义语言）** 来定义接口并自动生成存根代码。

**RPC 面临的三大核心挑战及解决方案**

| 挑战 | 解决方案 |
| --- | --- |
| **数据表示（大端 vs 小端问题）** | 引入**外部数据表示（XDR）**，客户端将数据转为机器无关的 XDR 格式再发送 |
| **调用语义（网络错误与可靠性）** | 采用"**最多一次（At most once）**"语义（通过时间戳去重）或"**正好一次（Exactly once）**"语义（需要额外的 ACK 确认） |
| **绑定（如何确定服务器的端口）** | 方案A：固定端口；方案B：动态绑定，通过"**交会服务程序 / 月老（Matchmaker）**"获取端口 |

**RPC 的实际应用（分布式文件系统）**

> [!example] DFS 实现
> RPC 机制是构建**分布式文件系统（DFS）**的基础。客户端通过 RPC 消息向服务器发送文件操作请求（`read`、`write`、`rename`、`delete` 等），服务端执行实际磁盘操作，将结果通过 RPC 返回给客户端。

## 3.6.3 管道

> [!summary] 基本定义
> 管道是 UNIX 系统最早使用的 IPC 机制。它为进程通信提供了一种**相对简单**的方法，但也存在**一定的局限性**。

**管道设计的四个属性**

- **方向性**：数据流是**单向**还是**双向**的？
- **并发传输**：如果支持双向，是**半双工**还是**全双工**？
- **进程关系**：通信的双方是否必须存在特定的关系（比如**父子进程**，或者是同一用户的进程）？
- **通信范围**：管道是**仅限于本地主机**使用，还是可以跨**网络**进行通信？

### 3.6.3.1 普通管道

**基本特性（硬性限制）**

> [!warning] 三大限制
> - **单向通信（半双工）**：只能一端写入，另一端读取。若需双向通信，必须创建两个管道。
> - **通信范围：仅限本地**：只能在**同一台计算机**上进行，不能跨越网络。
> - **进程关系限制**：通信双方必须具有**亲缘关系（父-子进程）**。通常由父进程创建管道，并通过 `fork()`（UNIX）或 `CreateProcess()`（Windows）让子进程继承管道的文件描述符或句柄。

**UNIX 系统的实现**

> [!mechanism] UNIX 普通管道流程
> 父进程先调用 `pipe()` 获得读端和写端，再 `fork()` 让子进程继承文件描述符。通信双方必须立即关闭各自不用的一端，然后通过 `read()` / `write()` 传输字节；只有所有写端都关闭后，读端才会得到 EOF。忘记关闭继承的写端是管道程序无法结束的常见原因。

> [!example] 对照代码
> [[MOC - 操作系统示例代码#第三章：进程与进程间通信|查看 POSIX 管道与 Windows 匿名管道]]。原始 Windows 代码虽然位于“命名管道”目录，实际调用 `CreatePipe()`，因此按匿名管道归类。

**Windows 系统的实现**

> [!mechanism] 匿名管道
> - **名称**：在 Windows 中被称为**匿名管道（anonymous pipe）**。
> - **创建**：使用 Windows API 函数 `CreatePipe()`。需要明确传递**读句柄**和**写句柄**。
> - **继承设置**：与 UNIX 自动继承不同，Windows 需要程序员显式设置继承属性。需要配置 `SECURITY_ATTRIBUTES` 结构允许句柄继承，并在 `CreateProcess()` 创建子进程时将子进程的**标准输入/输出重定向**到管道的对应端。

### 3.6.3.2 命名管道

**命名管道 vs 普通管道的核心提升**

| 特性 | 普通管道 | 命名管道 |
| --- | --- | --- |
| **持久性** | 随进程消亡而消失 | 作为文件系统对象持久存在，直到被显式删除 |
| **关系限制** | 必须具有亲缘关系 | 无此限制，允许任意进程访问 |
| **双向性** | 单向（半双工） | 支持双向通信 |

**UNIX 的实现：FIFO**

> [!mechanism] 特殊文件
> - **创建与使用**：通过系统调用 `mkfifo()` 创建，在文件系统中表现为一个特殊文件。使用标准的 `open()`、`read()`、`write()`、`close()` 进行操作。
> - **局限性**：仅支持**半双工**；仅限于同一台机器内的进程通信；只支持**字节流**数据传输。

**Windows 的实现：匿名管道 vs 命名管道**

> [!mechanism] Windows 命名管道
> 服务端以 `CreateNamedPipe()` 建立命名实例并等待连接，客户端以 `CreateFile()` 打开管道，双方通过 `ReadFile()` / `WriteFile()` 通信。命名管道可配置单工或双工、字节模式或消息模式，也可在允许的配置下跨主机使用；其名称和安全描述符决定谁能够连接，因此不能把“有名字”误认为“天然安全”。

---

## 章节间关联

本章内容与前两章紧密衔接，并为后续章节奠定基础：

- [[2.4 系统调用的类型#2.4.1 进程控制|进程控制]]（第二章）介绍了进程相关的系统调用，本章深入探讨进程的概念、状态管理和调度机制。
- [[2.4 系统调用的类型#2.4.5 通信|进程间通信]]（第二章）概述了消息传递和共享内存模型，本章详细阐述了这两种模型的实现细节和对比分析。
- [[第四章 多线程编程]]将深入探讨**线程**概念，扩展本章的进程模型。
- [[第六章 同步]]将讲解**信号量**和**互斥锁**，解决本章提到的共享内存同步问题。
- [[第八章 内存管理策略]]将讨论**内存管理**和**交换**技术，与本章的上下文切换和中期调度程序相关联。

> [!info] 章节导航
> 上一节：[[3.5 IPC系统例子]]　｜　章节：[[第三章 进程]]　｜　下一节：无

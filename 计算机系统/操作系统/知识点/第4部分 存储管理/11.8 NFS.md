---
domain: 计算机系统
type: 概念
status: 已完成
course: 操作系统
chapter: 11
section: "11.8"
order: 1108
cssclasses:
  - os-note
prerequisites:
  - "[[第十一章 文件系统实现]]"
  - "[[11.7 恢复]]"
tags:
  - 操作系统
  - 存储管理
source: Silberschatz, Operating System Concepts (10th Edition)
created: 2026-07-16
updated: 2026-07-16
---

# 11.8 NFS

本节聚焦于**NFS**，是[[第十一章 文件系统实现]]中的独立知识节点。

> [!important] NFS 核心概念
> 
> 网络文件系统（Network File System, NFS）通过 RPC 使客户端以类似本地文件操作的方式访问服务器导出的目录。它是 [[10.5 文件共享]] 中远程文件系统概念的一种具体实现；其缓存、认证、锁和一致性语义必须按 NFS 版本与配置理解。

## 11.8.1 概述

> [!info] NFS 架构
> 
> 客户端通过 VFS 将对远程挂载点的操作分派给 NFS 客户端模块；模块把请求编码为 RPC，服务器验证请求并在本地文件系统执行操作。服务器可同时服务多个客户端，一个客户端也可挂载多个服务器导出目录。

## 11.8.2 安装协议

> [!tip] 挂载协议流程
> 
> 挂载协议帮助客户端取得远程导出目录的初始文件句柄，并将其绑定到本地挂载点。
> 
> **文件句柄特性**：通常包含足以让服务器重新定位对象的信息；客户端不应把它当作跨服务器、跨重建或永久有效的通用文件名。

## 11.8.3 NFS 协议

> [!note] 协议演进
> 
> - **早期版本**：强调请求自包含和服务器无状态，便于服务器重启后的处理；
> - **后续版本**：引入更丰富的状态、认证和锁语义。
> 
> **安全警示**：无状态不等于没有安全要求，也不等于天然安全。每个请求仍必须通过适当的身份验证、完整性保护和授权检查。

## 11.8.4 路径名称转换

> [!important] 路径解析机制
> 
> 客户端逐段解析路径：先定位挂载点的根句柄，再请求服务器查找下一目录项，直至获得目标对象句柄。
> 
> **关键理解**：名称到句柄的转换会受服务器端重命名、删除、权限、缓存和文件系统重建影响，因此错误和重新验证是协议正常路径的一部分。

## 11.8.5 远程操作

> [!warning] 远程操作的挑战
> 
> 远程读写必须处理超时、重复 RPC、服务器故障和网络分区。
> 
> **关键机制**：对于可能重试的操作，协议与实现需要区分幂等和非幂等行为，并结合事务标识、缓存验证或恢复协议避免重复副作用。
> 
> **应用建议**：应用若依赖严格并发语义，应额外使用适当的锁或协作协议。

> [!info] 章节导航
> 上一节：[[11.7 恢复]]　｜　章节：[[第十一章 文件系统实现]]　｜　下一节：[[11.9 例子 - WAFL 文件系统]]

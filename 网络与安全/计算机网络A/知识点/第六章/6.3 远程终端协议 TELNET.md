---
domain: 网络与安全
type: 课程笔记
cssclasses:
  - network-note
status: 已完成
course: 计算机网络A
chapter: 6
section: "6.3"
order: 603
aliases:
  - TELNET
  - Network Virtual Terminal
  - 网络虚拟终端
prerequisites:
  - "[[5.3 传输控制协议 TCP 概述]]"
tags:
  - 应用层
  - TELNET
  - 远程登录
  - 计算机网络
created: 2026-07-16
updated: 2026-07-16
---

# 6.3 远程终端协议 TELNET

TELNET 用网络虚拟终端（NVT）屏蔽不同终端和操作系统的表示差异，并通过选项协商扩展功能。它是理解远程终端抽象的经典协议，但不提供现代远程管理所需的机密性和强认证。

> [!abstract] 一句话主线
> **本地和远端各自完成“本机格式 ↔ NVT”的转换，使异构终端能经同一种网络表示交换数据和控制命令。**

> [!tip] 阅读方式
> 先读“核心结构”掌握参与方、报文方向、状态与失败边界，再在“详细展开”中核对教材推导、报文格式和历史背景。

## 核心结构

### NVT 适配层

```mermaid
flowchart LR
    A["本地终端格式"] --> B["TELNET 客户：转为 NVT"]
    B --> C["TCP 连接"]
    C --> D["TELNET 服务器：转为远端格式"]
    D --> E["远端系统"]
```

| 参与方 | 主要职责 |
| --- | --- |
| TELNET 客户 | 捕获输入、编码为 NVT、显示服务器输出 |
| TELNET 服务器 | 把 NVT 映射为远端终端语义并返回结果 |
| 选项协商 | 双方协商回显、终端类型等扩展能力 |

> [!danger] 明文协议
> TELNET 的数据与认证信息通常缺少加密保护，无法提供机密性，也容易遭受窃听和篡改。它更适合作为协议史和受控实验知识；跨不可信网络的远程管理通常应使用具备加密、完整性和主机认证的方案。

## 详细展开

TELNET 是一个简单的远程终端协议 [RFC 854, STD8]。用户用 TELNET 就可在其所在地通过 TCP 连接注册（即登录）到远地的另一台主机上（使用主机名或 IP 地址）。TELNET 能将用户的击键传到远地主机，同时也能将远地主机的输出通过 TCP 连接返回到用户屏幕。这种服务是透明的，因为用户感觉到好像键盘和显示器是直接连在远地主机上的。因此，TELNET 又称为**终端仿真协议**。

TELNET 的 NVT 与选项协商仍有教学价值；但其明文传输不适合跨不可信网络的远程管理。

TELNET 也使用客户服务器方式。在本地系统运行 TELNET 客户进程，而在远地主机则运行 TELNET 服务器进程。和 FTP 的情况相似，服务器中的主进程等待新的请求，并产生从属进程来处理每一个连接。

![[Pasted image 20260716161157.png]]

TELNET 能够适应许多计算机和操作系统的差异。例如，对于文本中一行的结束，有的系统使用 ASCII 码的回车(CR)，有的系统使用换行(LF)，还有的系统使用两个字符，回车-换行(CR-LF)。又如，在中断一个程序时，许多系统使用 Control-C (`^C`)，但也有系统使用 ESC 按键。为了适应这种差异，TELNET 定义了数据和命令应怎样通过互联网。这些定义就是所谓的**网络虚拟终端 NVT (Network Virtual Terminal)**。图 6-6 说明了 NVT 的意义。客户软件把用户的击键和命令转换成 NVT 格式，并送交服务器。服务器软件把收到的数据和命令从 NVT 格式转换成远地系统所需的格式。向用户返回数据时，服务器把远地系统的格式转换为 NVT 格式，本地客户再从 NVT 格式转换到本地系统所需的格式。

NVT 的格式定义很简单。所有的通信都使用 8 位一个字节。在运转时，NVT 使用 7 位 ASCII 码传送数据，而当高位 1 时用作控制命令。ASCII 码共有 95 个可打印字符（如字母、数字、标点符号）和 33 个控制字符。所有可打印字符在 NVT 中的意义和在 ASCII 码中一样。但 NVT 只使用了 ASCII 码的控制字符中的几个。此外，NVT 还定义了两字符的 CR-LF 为标准的行结束控制符。当用户键入回车按键时，TELNET 的客户就把它转换为 CR-LF 再进行传输，而 TELNET 服务器要把 CR-LF 转换为远地机器的行结束字符。

TELNET 的**选项协商(Option Negotiation)**使 TELNET 客户和 TELNET 服务器可商定使用更多的终端功能，协商的双方是平等的。

---

上一节：[[6.2 文件传送协议 FTP 与 TFTP]]　｜　下一节：[[6.4 万维网与 HTTP]]　｜　章节入口：[[第六章 应用层]]

---
domain: 数据结构
type: 课程笔记
status: 已完成
created: 2026-07-17
updated: 2026-07-18
course: 数据结构
chapter: 第四章
section: "4.6"
order: 40600
aliases:
  - "案例分析与实现"
  - "4.6 案例分析与实现"
tags:
  - 数据结构
source: "严蔚敏, 吴伟民. 数据结构（C语言版）. 清华大学出版社. 第4章 串、数组和广义表"
moc: "[[MOC - 数据结构]]"
chapter_moc: "[[MOC - 第4章 串、数组和广义表]]"
cssclasses:
  - data-structure-note
---

# 4.6 案例分析与实现

> [!nav] 导航
> 上一知识点：[[4.05.02 广义表的存储结构]] · [[MOC - 第4章 串、数组和广义表|本章目录]] · [[MOC - 数据结构|课程总览]] · 下一知识点：[[4.07 LeetCode 算法练习题]]

## 案例 4.1：植物双生病毒感染检测

【案例分析】
植物 DNA 和病毒 DNA 是由字母组成的字符串序列，检测病毒 DNA 是否在植物 DNA 中出现，即字符串模式匹配问题，可用 BF 或 KMP 算法。但病毒 DNA 是环状的，需对传统算法改进。下面给出基于 BF 算法的方案。

【案例实现】
对每个待检测任务，设病毒 DNA 长度为 $m$。因病毒 DNA 环状，为线性取到每个长度为 $m$ 的模式串，将字符串长度扩为 $2m$，把病毒 DNA 连续存两次；循环 $m$ 次，依次取每个长度为 $m$ 的环状字符串作为模式串，植物 DNA 作为主串，调用 BF 算法。匹配成功则中止循环，表明感染；否则判断返回值输出 `"YES"` 或 `"NO"`。

【算法步骤】
① 从文件读取任务数 $\text{num}$。
② 循环 $\text{num}$ 次，对每对病毒/植物 DNA：
* 分别读取病毒 DNA 序列和植物 DNA 序列；
* 设标志变量 $\text{flag}$（初始 0，未匹配）；
* 将病毒 DNA 长度扩为 $2m$，连续存储两次；
* 循环 $m$ 次：取每个长度为 $m$ 的环状串作为模式串，植物 DNA 作为主串，调用 BF，结果赋给 $\text{flag}$；若 $\text{flag}$ 非 0 则中止；
* 退出循环后，$\text{flag}$ 非 0 输出 `"YES"`，否则 `"NO"`。

【算法描述】

```c
void Virus_detection()
{ // 利用 BF 算法实现病毒检测
    ifstream inFile("病毒感染检测输入数据.txt");
    ofstream outFile("病毒感染检测输出结果.txt");
    inFile >> num;                           // 读取待检测的任务数
    while (num--)
    {                                        // 依次检测每对病毒 DNA 和植物 DNA 是否匹配
        inFile >> Virus.ch + 1;              // 读取病毒 DNA 序列，字符串从下标 1 开始存放
        Vir = Virus.ch;
        flag = 0;                            // 用来标识是否匹配，初始为 0，匹配后为非 0
        m = Virus.length;                    // 病毒 DNA 序列的长度是 m
        for (i = m + 1, j = 1; j <= m; j++)
            Virus.ch[i++] = Virus.ch[j];     // 将病毒字符串的长度扩大 2 倍
        Virus.ch[2 * m + 1] = '\0';          // 添加结束符号
        for (i = 0; i < m; i++)              // 取得每个长度为 m 的病毒 DNA 环状串 temp
        {
            for (j = 1; j <= m; j++) temp.ch[j] = Virus.ch[i + j];
            temp.ch[m + 1] = '\0';           // 添加结束符号
            flag = Index_BF(Person, temp, 1); // 模式匹配
            if (flag) break;                 // 匹配即可退出循环
        }
        if (flag)
            outFile << Vir + 1 << " " << Person.ch + 1 << " " << "YES" << endl;
        else
            outFile << Vir + 1 << " " << Person.ch + 1 << " " << "NO" << endl;
    } // while
}
```

> [!example] 算法 4.5 病毒感染检测
> 基于 BF 算法，将环状病毒 DNA 展开为 $2m$ 长度后逐段取模式串匹配。对每个任务需执行 $m$ 次匹配，植物 DNA 长 $n$，单次 BF 为 $O(m \times n)$，故单任务时间复杂度 $O(m \times m \times n)$；任务数 $\text{num}$ 时整体 $O(\text{num} \times m \times m \times n)$，较高。改用 KMP 可显著提升效率。

模式匹配算法在现代生物学（基因序列分析、生物标志物识别、蛋白质序列分析）与网络安全（入侵检测系统 Snort 规则匹配）中均至关重要。KMP 还有 BM、Horspool 等改进版本，可按需选用。

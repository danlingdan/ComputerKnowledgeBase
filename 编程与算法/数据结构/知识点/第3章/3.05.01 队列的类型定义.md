---
domain: 数据结构
type: 课程笔记
status: 已完成
created: 2026-07-17
updated: 2026-07-18
course: 数据结构
chapter: 第三章
section: "3.5.1"
order: 30501
aliases:
  - "队列的类型定义"
  - "3.5.1 队列的类型定义"
tags:
  - 数据结构
source: "严蔚敏, 吴伟民. 数据结构（C语言版）. 清华大学出版社. 第3章 栈和队列"
moc: "[[MOC - 数据结构]]"
chapter_moc: "[[MOC - 第3章 栈和队列]]"
cssclasses:
  - data-structure-note
---

# 3.5.1 队列的类型定义

> [!nav] 导航
> 上一知识点：[[3.04.04 利用栈将递归转换为非递归的方法]] · [[MOC - 第3章 栈和队列|本章目录]] · [[MOC - 数据结构|课程总览]] · 下一知识点：[[3.05.02 顺序队的表示和实现]]

> [!topic] 所属主题
> [[MOC - 第3章 栈和队列#3.5 队列的表示和实现|3.5 队列的表示和实现]]

> [!definition] 队列（Queue）
> 队列是一种**先进先出**（First In First Out，FIFO）的线性表。它只允许在表的一端插入数据元素，而在另一端删除数据元素：插入端称为**队尾**（rear），删除端称为**队头**（front）。
> 即最先入队的元素最先出队，符合"排队的公平性"；其与栈的区别仅在于删除操作发生在表的头部（队头）。

下面给出队列的抽象数据类型定义：

> [!info] 队列的抽象数据类型 ADT Queue
> ```c
> ADT Queue {
>     数据对象：D={a_i | a_i ∈ ElemSet, i=1,2,···,n, n ≥ 0}
>     数据关系：R={< a_{i-1}, a_i > | a_{i-1}, a_i ∈ D, i=2,···,n}
>               约定其中 a_1 端为队头，a_n 端为队尾。
>     基本操作：
>        InitQueue(&Q)       操作结果：构造一个空队列 Q。
>        DestroyQueue(&Q)    初始条件：队列 Q 已存在。操作结果：队列 Q 被销毁，不再存在。
>        ClearQueue(&Q)      初始条件：队列 Q 已存在。操作结果：将 Q 清为空队列。
>        QueueEmpty(Q)       初始条件：队列 Q 已存在。操作结果：若 Q 为空队列返回 true，否则 false。
>        QueueLength(Q)      初始条件：队列 Q 已存在。操作结果：返回 Q 的元素个数，即队列长度。
>        GetHead(Q)          初始条件：Q 为非空队列。操作结果：返回 Q 的队头元素。
>        EnQueue(&Q, e)      初始条件：队列 Q 已存在。操作结果：插入元素 e 为 Q 的新的队尾元素。
>        DeQueue(&Q, &e)     初始条件：Q 为非空队列。操作结果：删除 Q 的队头元素，并用 e 返回其值。
>        QueueTraverse(Q)    初始条件：Q 已存在且非空。操作结果：从队头到队尾依次访问每个数据元素。
> } ADT Queue
> ```

和栈类似，本书后文引用的队列均为如上定义的队列类型，数据元素类型在应用程序内定义。队列也有两种存储表示：**顺序表示**（顺序队）和**链式表示**（链队）。

---
domain: 数据结构
type: 课程笔记
status: 已完成
created: 2026-07-17
updated: 2026-07-18
course: 数据结构
chapter: 第三章
section: "3.3.3"
order: 30303
aliases:
  - "链栈的表示和实现"
  - "3.3.3 链栈的表示和实现"
tags:
  - 数据结构
source: "严蔚敏, 吴伟民. 数据结构（C语言版）. 清华大学出版社. 第3章 栈和队列"
moc: "[[MOC - 数据结构]]"
chapter_moc: "[[MOC - 第3章 栈和队列]]"
cssclasses:
  - data-structure-note
---

# 3.3.3 链栈的表示和实现

> [!nav] 导航
> 上一知识点：[[3.03.02 顺序栈的表示和实现]] · [[MOC - 第3章 栈和队列|本章目录]] · [[MOC - 数据结构|课程总览]] · 下一知识点：[[3.04.01 采用递归算法解决的问题]]

> [!topic] 所属主题
> [[MOC - 第3章 栈和队列#3.3 栈的表示和实现|3.3 栈的表示和实现]]

![[Attachments/Pasted image 20260717163209.png]]

> [!definition] 链栈（Linked Stack）
> 链栈是采用链式存储结构实现的栈，通常用单链表表示（如图 3.4）。其结点结构与单链表相同，用 `StackNode` 表示。由于主要操作在栈顶插入和删除，以链表头部作为栈顶最方便，且没必要像单链表那样附加头结点。

```c
// - - - - - 链栈的存储结构 - - - - -
typedef struct StackNode
{
    ElemType data;
    struct StackNode *next;
}StackNode, *LinkStack;
```

下面给出链栈部分操作的实现。

**1. 初始化**

> [!example] 算法 3.5 链栈的初始化
> 构造一个空栈；因无需头结点，直接将栈顶指针置空即可。
> 【算法描述】
> ```c
> Status InitStack(LinkStack &S)
> {// 构造一个空栈S，栈顶指针置空
>     S=NULL;
>     return OK;
> }
> ```

**2. 入栈**

> [!example] 算法 3.6 链栈的入栈
> 与顺序栈不同，链栈入栈前不需判断栈满，只需为新元素动态分配一个结点空间（如图 3.5）。
> 【算法步骤】
> ① 为入栈元素 `e` 分配空间，用指针 `p` 指向。
> ② 将新结点数据域置为 `e`。
> ③ 将新结点插入栈顶。
> ④ 修改栈顶指针为 `p`。
> 【算法描述】
> ```c
> Status Push(LinkStack &S, SElemType e)
> {// 在栈顶插入元素 e
>     p=new StackNode;                    // 生成新结点
>     p->data=e;                          // 将新结点数据域置为 e
>     p->next=S;                          // 将新结点插入栈顶
>     S=p;                                // 修改栈顶指针为 p
>     return OK;
> }
> ```

![[Attachments/Pasted image 20260717163216.png]]

**3. 出栈**

> [!example] 算法 3.7 链栈的出栈
> 与顺序栈一样，出栈前需判断栈是否为空；不同处在于出栈后需释放栈顶结点的空间（如图 3.6）。
> 【算法步骤】
> ① 判断栈是否为空，若空则返回 `ERROR`。
> ② 将栈顶元素赋给 `e`。
> ③ 临时保存栈顶结点空间，以备释放。
> ④ 修改栈顶指针，指向新的栈顶元素。
> ⑤ 释放原栈顶结点的空间。
> 【算法描述】
> ```c
> Status Pop(LinkStack &S, SElemType &e)
> {// 删除 S 的栈顶元素，用 e 返回其值
>     if(S==NULL) return ERROR;           // 栈空
>     e=S->data;                          // 将栈顶元素赋给 e
>     p=S;                                // 用 p 临时保存栈顶元素空间，以备释放
>     S=S->next;                          // 修改栈顶指针
>     delete p;                           // 释放原栈顶元素的空间
>     return OK;
> }
> ```

![[Attachments/Pasted image 20260717163225.png]]

**4. 取栈顶元素**

> [!example] 算法 3.8 取链栈的栈顶元素
> 与顺序栈一样，当栈非空时返回当前栈顶元素的值，栈顶指针 `S` 保持不变。
> 【算法描述】
> ```c
> SElemType GetTop(LinkStack S)
> {// 返回 S 的栈顶元素，不修改栈顶指针
>     if(S!=NULL)                         // 栈非空
>         return S->data;                 // 返回栈顶元素的值，栈顶指针不变
> }
> ```

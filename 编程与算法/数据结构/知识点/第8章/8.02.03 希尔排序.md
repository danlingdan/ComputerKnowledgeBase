---
domain: 数据结构
type: 课程笔记
status: 已完成
created: 2026-07-18
updated: 2026-07-18
course: 数据结构
chapter: 第八章
section: "8.2.3"
order: 80203
aliases:
  - "希尔排序"
  - "8.2.3 希尔排序"
tags:
  - 数据结构
source: "严蔚敏, 吴伟民. 数据结构（C语言版）. 清华大学出版社. 第8章 排序"
moc: "[[MOC - 数据结构]]"
chapter_moc: "[[MOC - 第8章 排序]]"
cssclasses:
  - data-structure-note
---

# 8.2.3 希尔排序

> [!nav] 导航
> 上一知识点：[[8.02.02 折半插入排序]] · [[MOC - 第8章 排序|本章目录]] · [[MOC - 数据结构|课程总览]] · 下一知识点：[[8.03.01 冒泡排序]]

> [!topic] 所属主题
> [[MOC - 第8章 排序#8.2 插入排序|8.2 插入排序]]

> [!definition] 希尔排序（Shell's Sort）
> 又称"缩小增量排序"（Diminishing Increment Sort），是插入排序的一种，由 D.L. Shell 于 1959 年提出。它从"减少记录个数"和"序列基本有序"两方面改进直接插入排序。

## 算法 8.3 希尔排序

> [!info] **算法步骤**
> 采用分组插入：先将整个序列分割成若干组减少数据量，各组分别直接插入排序，再增加每组数据量重新分组；经几次分组排序使序列"基本有序"后，对全体记录做一次直接插入排序。
> 希尔按"增量"分组（非逐段分割）：
> ① 第一趟取增量 $d_1\ (d_1 < n)$，间隔为 $d_1$ 的记录同组，各组直接插入排序。
> ② 第二趟取 $d_2\ (d_2 < d_1)$ 重复。
> ③ 直至 $d_t = 1\ (d_t < d_{t-1} < \cdots < d_2 < d_1)$，全体记录同组直接插入排序。

> [!example] **例 8.2** 序列 $\{49, 38, 65, 97, 76, 13, 27, 49, 55, 04\}$ 的希尔排序过程（增量取 5、3、1）。
>
> ![[Attachments/Pasted image 20260718224252.png]]


**【算法描述】**

```c
void ShellInsert(SqList &L, int dk)
{ // 对顺序表 L 进行一趟增量为 dk 的希尔插入排序
    for(i=dk+1; i<=L.length; ++i)
        if(L.r[i].key < L.r[i-dk].key)      // 需将 L.r[i] 插入有序增量子表
        {
            L.r[0]=L.r[i];                  // 暂存在 r[0] 中
            for(j=i-dk; j>0 && L.r[0].key<L.r[j].key; j-=dk)
                L.r[j+dk]=L.r[j];           // 记录后移，直到找到插入位置
            L.r[j+dk]=L.r[0];               // 将 r[0] 即原 r[i]，插入正确位置
        } // if
}

void ShellSort(SqList &L, int dt[], int t)
{ // 按增量序列 dt[0..t-1] 对顺序表 L 进行 t 趟希尔排序
    for(k=0; k<t; ++k)
        ShellInsert(L, dt[k]);              // 一趟增量为 dt[k] 的希尔插入排序
}
```

> [!abstract] **算法分析**
> **（1）时间复杂度**：关键字较小的记录可跳跃式移动，最后一趟开始时序列通常已基本有序，因此实践中常优于直接插入排序。希尔排序不存在脱离增量序列的统一复杂度：采用不同增量序列时，其上界和实际性能均会变化；分析时必须同时说明所用增量序列，不能把经验值 $O(n^{1.3})$ 当作通用结论。
> **（2）空间复杂度**：只需一个辅助空间 `r[0]`，为 $O(1)$。

> [!tip] **算法特点**
> （1）记录跳跃式移动，方法**不稳定**。（2）只能用于顺序结构。（3）增量序列应使各值除 1 外无公因子，且最后一个增量必须为 1。（4）比较与移动次数均少于直接插入排序，$n$ 越大越明显，适合无序且 $n$ 较大的情况。

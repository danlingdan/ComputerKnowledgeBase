---
domain: 数据结构
type: 课程笔记
status: 已完成
created: 2026-07-18
updated: 2026-07-18
course: 数据结构
chapter: 第八章
section: "8.3.2"
order: 80302
aliases:
  - "快速排序"
  - "8.3.2 快速排序"
tags:
  - 数据结构
source: "严蔚敏, 吴伟民. 数据结构（C语言版）. 清华大学出版社. 第8章 排序"
moc: "[[MOC - 数据结构]]"
chapter_moc: "[[MOC - 第8章 排序]]"
cssclasses:
  - data-structure-note
---

# 8.3.2 快速排序

> [!nav] 导航
> 上一知识点：[[8.03.01 冒泡排序]] · [[MOC - 第8章 排序|本章目录]] · [[MOC - 数据结构|课程总览]] · 下一知识点：[[8.04.01 简单选择排序]]

> [!topic] 所属主题
> [[MOC - 第8章 排序#8.3 交换排序|8.3 交换排序]]

> [!definition] 快速排序（Quick Sort）
> 由冒泡排序改进而来。冒泡只比较相邻记录，一次交换只消除一个逆序；快速排序通过一次（不相邻）交换可消除多个逆序，大幅加快速度。

## 算法 8.5 快速排序

> [!info] **算法步骤**
> 任取一个记录（通常取第一个）作枢轴（pivot），关键字为 `pivotkey`。一趟排序后，小于 `pivotkey` 的交换到前面、大于的交换到后面，枢轴落于分界处，原表分成两个子表；再对左右子表重复，直至每子表只有一个记录。
> 一趟快速排序：
> ① 取首记录为枢轴，暂存于 `r[0]`；设指针 `low`、`high` 初值指向表下界、上界。
> ② 从右向左找第一个小于 `pivotkey` 的记录移到 `low` 处。
> ③ 从左向右找第一个大于 `pivotkey` 的记录移到 `high` 处。
> ④ 重复 ②③ 直至 `low == high`，此处即枢轴最终位置，表被分为两个子表。

> [!example] **例 8.4** 序列 $\{49, 38, 65, 97, 76, 13, 27, 49\}$ 的快速排序过程。
>
> ![[Attachments/Pasted image 20260718224323.png]]
> ![[Attachments/Pasted image 20260718224335.png]]
> *图 8.4 快速排序过程（a）一趟划分；（b）整个递归过程*

**【算法描述】**

```c
int Partition(SqList &L, int low, int high)
{ // 对顺序表 L 中的子表 r[low..high] 进行一趟排序，返回枢轴位置
    L.r[0]=L.r[low];                    // 用子表的第一个记录作为枢轴记录
    pivotkey=L.r[low].key;              // 枢轴记录关键字保存在 pivotkey 中
    while(low<high)                     // 从表的两端交替地向中间查找
    {
        while(low<high && L.r[high].key>=pivotkey) --high;
        L.r[low]=L.r[high];             // 将比枢轴记录小的记录移到低端
        while(low<high && L.r[low].key<=pivotkey) ++low;
        L.r[high]=L.r[low];             // 将比枢轴记录大的记录移到高端
    } // while
    L.r[low]=L.r[0];                    // 枢轴记录到位
    return low;                         // 返回枢轴位置
}

void QSort(SqList &L, int low, int high)
{ // 调用前置初值：low=1; high=L.length;
  // 对顺序表 L 中的子表 L.r[low..high] 进行快速排序
    if(low<high) {                      // 长度大于 1
        pivotloc=Partition(L, low, high); // 将 L.r[low..high] 一分为二
        QSort(L, low, pivotloc-1);      // 对左子表递归排序
        QSort(L, pivotloc+1, high);     // 对右子表递归排序
    }
}

void QuickSort(SqList &L)
{ // 对顺序表 L 进行快速排序
    QSort(L, 1, L.length);
}
```

> [!tip] **快速排序递归树（例 8.4 序列 $\{49,38,65,97,76,13,27,49\}$）**
>
> ```mermaid
> graph TD
>     A["{49,38,65,97,76,13,27,49}<br/>pivot = 49"] --> B["左: {27,38,13}<br/>pivot = 27"]
>     A --> C["右: {76,97,65,49}<br/>pivot = 76"]
>     B --> B1["左: {13}"]
>     B --> B2["右: {38}"]
>     C --> C1["左: {49,65}<br/>pivot = 49"]
>     C --> C2["右: {97}"]
>     C1 --> C1a["左: {}"]
>     C1 --> C1b["右: {65}"]
> ```

> [!abstract] **算法分析**
> **（1）时间复杂度**：趟数取决于递归树深度。
> 最好情况：每趟均匀二分，对 $n$ 个元素定位需 $O(n)$，递推 $T(n) = Cn + 2T(n/2) \le n\log_2 n + nT(1) \approx O(n\log_2 n)$。
> 最坏情况：序列已序时递归树退化为单支，需 $n-1$ 趟，比较次数 $KCN = \frac{n(n-1)}{2} \approx \frac{n^2}{2}$，速度退化到简单排序水平。可用"三者取中"规则（首、尾、中三个记录取关键字居中者作枢轴）避免。平均时间复杂度为 $O(n\log_2 n)$。
> **（2）空间复杂度**：递归需要栈，最好情况 $O(\log_2 n)$，最坏情况 $O(n)$。

> [!tip] **算法特点**
> （1）记录非顺次移动，方法**不稳定**。（2）需定位上下界，适合顺序结构，难用于链式结构。（3）$n$ 较大时平均最快，适合无序且 $n$ 较大的情况。

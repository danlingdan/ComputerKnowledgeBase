---
domain: 数据结构
type: 课程笔记
status: 已完成
created: 2026-07-17
updated: 2026-07-18
course: 数据结构
chapter: 第六章
section: "6.4.2"
order: 60402
aliases:
  - "邻接表"
  - "6.4.2 邻接表"
tags:
  - 数据结构
source: "严蔚敏, 吴伟民. 数据结构（C语言版）. 清华大学出版社. 第6章 图"
moc: "[[MOC - 数据结构]]"
chapter_moc: "[[MOC - 第6章 图]]"
cssclasses:
  - data-structure-note
---

# 6.4.2 邻接表

> [!nav] 导航
> 上一知识点：[[6.04.01 邻接矩阵]] · [[MOC - 第6章 图|本章目录]] · [[MOC - 数据结构|课程总览]] · 下一知识点：[[6.04.03 十字链表]]

> [!topic] 所属主题
> [[MOC - 第6章 图#6.4 图的存储结构|6.4 图的存储结构]]

> [!definition] 邻接表
> 邻接表（Adjacency List）是图的链式存储结构：对每个顶点 $v_i$ 建一个单链表，存放与其邻接的顶点。由**表头结点表**（顺序存储）和**边表**组成：
> - **表头结点**：`data`（顶点信息）+ `firstarc`（指向第一条边）。
> - **边结点**：`adjvex`（邻接点位置）+ `info`（权值等）+ `nextarc`（下一条边）。

![[Attachments/Pasted image 20260717192413.png]]
![[Attachments/Pasted image 20260717192422.png]]

无向图 $G_1$、$G_2$ 的邻接表分别见图 6.12(a)(b)；有向图 $G_1$ 的逆邻接表见图 6.12(c)（便于求入度）。

存储表示：

```c
//-----图的邻接表存储表示-----
#define MVNum 100                       // 最大顶点数
typedef struct ArcNode                  // 边结点
{
    int adjvex;                         // 该边所指向的顶点的位置
    struct ArcNode * nextarc;           // 指向下一条边的指针
    OtherInfo info;                     // 和边相关的信息
}ArcNode;
typedef struct VNode                    // 顶点信息
{
    VerTexType data;                    // 指向第一条依附该顶点的边的指针
    ArcNode * firstarc;
}VNode, AdjList[MVNum];                 // AdjList 表示邻接表类型
typedef struct
{
    AdjList vertices;                   // 邻接表
    int vexnum, arcnum;                 // 图的当前顶点数和边数
}ALGraph;
```

## 1. 采用邻接表表示法创建无向图

> [!example] 算法 6.2 采用邻接表表示法创建无向图
> **【算法步骤】**
> ① 输入总顶点数和总边数。
> ② 输入点信息存入顶点表，表头指针域初始化为 NULL。
> ③ 创建邻接表：输入每条边依附的两顶点，确定序号 $i,j$ 后，将边结点分别头插到 $v_i$、$v_j$ 的边链表。
> **【算法描述】**
> ```c
> Status CreateUDG(ALGraph &G)
> { // 采用邻接表表示法，创建无向图 G
>     cin>>G.vexnum>>G.arcnum;            // 输入总顶点数，总边数
>     for(i=0;i<G.vexnum;++i)
>     {
>         cin>>G.vertices[i].data;        // 输入顶点值
>         G.vertices[i].firstarc=NULL;    // 初始化表头结点的指针域为 NULL
>     }//for
>     for(k=0;k<G.arcnum;++k)             // 输入各边，构造边表
>     {
>         cin>>v1>>v2;                    // 输入一条边依附的两个顶点
>         i=LocateVex(G, v1); j=LocateVex(G, v2);
>         p1=new ArcNode;                 // 生成一个新的边结点 *p1
>         p1->adjvex=j;                   // 邻接点序号为 j
>         p1->nextarc=G.vertices[i].firstarc; G.vertices[i].firstarc=p1;
>         p2=new ArcNode;                 // 生成另一个对称的新的边结点 *p2
>         p2->adjvex=i;
>         p2->nextarc=G.vertices[j].firstarc; G.vertices[j].firstarc=p2;
>     }//for
>     return OK;
> }
> ```
> **【算法分析】** 时间复杂度为 $O(n \times e)$。

> [!note] 邻接表表示法的优缺点
> **优点**：① 便于增删顶点；② 统计边数 $O(n+e)$；③ 空间复杂度 $O(n+e)$，适合稀疏图。
> **缺点**：① 判断两顶点间是否有边需查边表，最坏 $O(e)$；② 有向图求入度困难（需遍历各边表），逆邻接表则反之。

> [!warning] 邻接表表示不唯一
> 一个图的邻接矩阵唯一，但邻接表不唯一——各边表结点的链接次序取决于建表算法与边的输入次序。

---
domain: 数据结构
type: 课程笔记
status: 已完成
created: 2026-07-17
updated: 2026-07-18
course: 数据结构
chapter: 第六章
section: "6.4.4"
order: 60404
aliases:
  - "邻接多重表"
  - "6.4.4 邻接多重表"
tags:
  - 数据结构
source: "严蔚敏, 吴伟民. 数据结构（C语言版）. 清华大学出版社. 第6章 图"
moc: "[[MOC - 数据结构]]"
chapter_moc: "[[MOC - 第6章 图]]"
cssclasses:
  - data-structure-note
---

# 6.4.4 邻接多重表

> [!nav] 导航
> 上一知识点：[[6.04.03 十字链表]] · [[MOC - 第6章 图|本章目录]] · [[MOC - 数据结构|课程总览]] · 下一知识点：[[6.05.01 深度优先搜索]]

> [!topic] 所属主题
> [[MOC - 第6章 图#6.4 图的存储结构|6.4 图的存储结构]]

> [!definition] 邻接多重表
> 邻接多重表（Adjacency Multilist）是无向图的另一种链式存储结构。每条边用一个结点（而非邻接表中的两个），便于对边做标记 / 删除等操作。
> - **边结点 EBox**：`mark`（访问标记）、`ivex`/`jvex`（边依附的两顶点）、`ilink`（依附 ivex 的下一条边）、`jlink`（依附 jvex 的下一条边）、`info`。
> - **顶点结点 VexBox**：`data`、`firstedge`（第一条依附该顶点的边）。

![[Attachments/Pasted image 20260717192451.png]]
![[Attachments/Pasted image 20260717192457.png]]

```c
//-----无向图的邻接多重表存储表示-----
#define MAX_VERTEX_NUM 20
typedef enum {unvisited, visited} VisitIf;
typedef struct EBox
{
    VisitIf mark;                       // 访问标记
    int ivex, jvex;                     // 该边依附的两个顶点的位置
    struct EBox *ilink, *jlink;         // 分别指向依附这两个顶点的下一条边
    InfoType *info;                     // 该边信息指针
}EBox;
typedef struct VexBox
{
    VertexType data;
    EBox *firstedge;                    // 指向第一条依附该顶点的边
}VexBox;
typedef struct
{
    VexBox adjmulist[MAX_VERTEX_NUM];
    int vexnum, edgenum;                // 无向图的当前顶点数和边数
}AMLGraph;
```

---
